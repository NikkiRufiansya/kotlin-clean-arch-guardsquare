package o;

import android.text.Editable;
import android.view.inputmethod.EditorInfo;
import android.view.inputmethod.InputConnection;

/* loaded from: classes.dex */
public class linkToDeath$MenuHostHelper$$ExternalSyntheticLambda0 {
    public boolean setX(InputConnection inputConnection, Editable editable, int i, int i2, boolean z) {
        return setScrollContainer.setIconTintList(inputConnection, editable, i, i2, z);
    }

    public void setIconTintList(EditorInfo editorInfo) {
        if (setScrollContainer.setX()) {
            setScrollContainer.setIconTintList().setIconTintList(editorInfo);
        }
    }
}